# URL base de la API (incluye /api/v1).
# Con docker-compose no hace falta tocar esto: nginx redirige /api/v1
# internamente al contenedor del backend, así que la ruta relativa alcanza.
# Si corrés el frontend suelto con `npm run dev` (sin Docker), poné la URL
# completa del backend, por ejemplo:
# VITE_API_URL=http://localhost:3000/api/v1
VITE_API_URL=/api/v1

# Conector modular para el alta de bandas — todavía no hay pantallas propias
# en la app, así que este link (si se completa) apunta a un formulario o
# página externa. Dejalo vacío para mostrar "muy pronto" en su lugar.
VITE_BAND_SIGNUP_URL=
